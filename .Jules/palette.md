## 2024-05-01 - Modal Accessibility Improvements
**Learning:** Common accessible modal patterns (`role="dialog"`, `aria-modal="true"`, dynamic `aria-labelledby` with `useId()`, and focused close buttons) were missing from the core generic `<Modal>` component.
**Action:** Apply these standardized WAI-ARIA and focus states across other UI overlay components automatically in future design updates.
